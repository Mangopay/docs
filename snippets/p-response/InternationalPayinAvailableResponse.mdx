<ResponseField
  name="InternationalPayinAvailable"
  type="boolean"
>
Whether or not international bank wires can be made to this account.

Returned `false` if `Country` is `US` or `CA`.
</ResponseField>