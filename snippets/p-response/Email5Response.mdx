import FormatAValidEmailAddress from '/snippets/rules/format-a-valid-email-address.mdx';

<ResponseField name="Email" type="string" >  
<FormatAValidEmailAddress />    
Required if `UserCategory` is `OWNER`. The `LegalRepresentative.Email` is used during the SCA session.

The individual's email address.

Returned null if `UserCategory` is `PAYER`.
</ResponseField>