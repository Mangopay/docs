```ruby Ruby
require 'mangopay'

MangoPay.configure do |client|
  client.preproduction = true
  client.client_id = 'your-mangopay-client-id'
  client.client_apiKey = 'your-mangopay-api-key'
  client.log_file = File.join(Dir.pwd, 'mangopay.log')
end

def createNaturalUserSca(naturalUserScaObject)
  begin
    response = MangoPay::NaturalUserSca.create(naturalUserScaObject)
    puts response
    return response
  rescue MangoPay::ResponseError => error
    puts "Failed to create User: #{error.message}"
    puts "Error details: #{error.details}"
    return false
  end
end

myNaturalUserSca = {
  UserCategory: 'OWNER',
  TermsAndConditionsAccepted: true,
  FirstName: 'Alex',
  LastName: 'Smith',
  Email: 'alex.smith@example.com',
  Address: {
    AddressLine1: '3 rue de la Cité',
    AddressLine2: 'Appartement 7',
    City: 'Paris',
    Region: 'Île-de-France',
    PostalCode: '75004',
    Country: 'FR'
  },
  Birthday: 652117514,
  CountryOfResidence: 'FR',
  Nationality: 'FR',
  Tag: 'Created using the Mangopay Ruby SDK',
  PhoneNumber: '0611111111',
  PhoneNumberCountry: 'FR'
}

createNaturalUserSca(myNaturalUserSca)
```