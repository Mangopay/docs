  
<ParamField body="CompanyNumber" type="string" >     
The registration number of the entity, assigned by the relevant national authority. For information on the expected format for a specific country, see the Requirements for legal users. To validate the format of a number before submitting documents for verification, use Validate the format of User data.  
This value is `null` if the `UserCategory` is `PAYER`.  
</ParamField>