export const Signal = ({content}) => (
    <Tooltip tip="Attribute of a user’s session detected by the fraud prevention profiler.">
        <a href="/concepts/glossary/signal">{content}</a>
    </Tooltip>
);