export const Mit = ({content}) => (
    <Tooltip tip="A transaction, linked to a preceding CIT, that is initiated by the platform without the cardholder.">
        <a href="/concepts/glossary/mit">{content}</a>
    </Tooltip>
);