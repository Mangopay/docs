export const Kyc = ({content}) => (
    <Tooltip tip="Term used to refer to the identity verification of natural persons (i.e., individuals).">
        <a href="/concepts/glossary/kyc">{content}</a>
    </Tooltip>
);