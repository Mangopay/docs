export const MangopayDashboard = ({ content }) => (
  <Tooltip tip="Mangopay interface enabling platform operators to manage their payment operations and centralize their Mangopay experience.">
    {content}
  </Tooltip>
);