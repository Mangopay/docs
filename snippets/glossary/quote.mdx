export const Quote = ({content}) => (
    <Tooltip tip="Agreement to freeze a conversion rate, for a given currency pair, for a duration of time">
        <a href="/glossary/quote">{content}</a>
    </Tooltip>
);