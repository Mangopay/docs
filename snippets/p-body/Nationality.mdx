<ParamField body="Nationality" type="string" >
**Allowed values:** Country code in the <a href="/api-reference/overview/data-formats" target="_blank">ISO 3166-1 alpha-2 format</a>.

Required if `UserCategory` is `OWNER`. Returned `null` if `UserCategory` is `PAYER`.

The nationality of the user.
</ParamField>