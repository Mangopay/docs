<ParamField query="RecipientScope" type="string">
**Possible values:** `PAYIN` `PAYOUT`

**Default value:** `PAYOUT`

By default, this endpoint returns only recipients with the `RecipientScope` value of `PAYOUT` unless `PAYIN` is specified as the query parameter value. It is also not possible to combine the query values.

The recipient scope:
- `PAYOUT` – Usable for payouts and in pay-in use cases. The User must have the `UserCategory` of `OWNER`.
- `PAYIN` - Not usable for payouts but only usable for pay-in use cases, such as direct debit and refunds using payouts. The User can have the `UserCategory` of `PAYER` or `OWNER`.
</ParamField>