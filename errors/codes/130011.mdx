---
title: '130011'  
description: '<small>&#8249; [All error codes](/errors/codes#instant-payouts)</small>'
---
  
<table>
<tbody>
<tr>
<th class="header"><strong>Error message</strong></th>
<td class="table-content">Manual review required - not compatible with payout mode requested</td>
</tr>
<tr>
<th class="header"><strong>Description</strong></th>
<td class="table-content">A manual review is required for this payout, so the instant payout could not be processed.</td>
</tr>
<tr>
<th class="header"><strong>Categories</strong></th>
<td class="table-content">  
<div class="label">Payout</div>  
<div class="label">Instant payment</div>
</td>
</tr>
</tbody>
</table>  

#### Causes  

Some payouts are subject to manual review by Mangopay's teams for reasons of risk management or AML/CFT.   

This error ùay occur when a manual review is required on a EUR instant payout via SCT Inst, regardless of whether the fallback to SCT was requested or not.

This error may occur for payouts with the `PayoutModeRequested` of both `INSTANT_PAYMENT` (with fallback) and `INSTANT_PAYMENT_ONLY` (without fallback).

#### What to do  

For both payout modes, if the payout's `Status` is `FAILED` then you need to create another payout with the `PayoutModeRequest` set to `STANDARD` so that the manual review can be performed by Mangopay's teams.