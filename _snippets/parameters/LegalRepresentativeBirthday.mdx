<ParamField body="LegalRepresnetativeBirthday" type="timestamp">
  **Required if :** `UserCategory` is `OWNER`
  
  The date of birth of the entity’s legal representative.
  
  This value is `null` if the `UserCategory` is `PAYER`.
</ParamField>