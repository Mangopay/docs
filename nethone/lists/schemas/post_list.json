{
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema",
  "required": ["name", "fields"],
  "properties": {
    "name": {
      "$ref": "./common/list_name.json",
      "type": "string",
      "description": "Name used to identify this list. Must be unique in your application"
    },
    "display_name": {
      "$ref": "./common/list_name.json",
      "description": "Name used when displaying this list in panel. If unspecified, it is set to equal name"
    },
    "fields": {
      "type": "array",
      "title": "List of fields",
      "description": "A list of fields (columns) this list will contain. Various datatypes can be used in different matchers in list-based rules.",
      "items": {
        "$ref": "./common/field_definition.json"
      }
    }
  }
}
