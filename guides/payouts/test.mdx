---
title: Payout routes per currency
---

export const CurrencyPayoutRoutesDropdown = () => {
  const paymentData = {
    AED: { local: [], international: [{ rail: "SWIFT", cutoffTime: "10:30", deliveryTime: "1-3 Business Days" }], },
    AUD: { local: [], international: [{ rail: "SWIFT", cutoffTime: "11:00", deliveryTime: "1-3 Business Days" }], },
    CAD: { local: [{ rail: "EFT", cutoffTime: "17:00", deliveryTime: "1-2 Business Days" }], international: [{ rail: "SWIFT", cutoffTime: "17:00, 12:00", deliveryTime: "1-3 Business Days" }], },
    CHF: { local: [{ rail: "SIC", cutoffTime: "16:15", deliveryTime: "Same Day" }], international: [{ rail: "SWIFT", cutoffTime: "12:30", deliveryTime: "1-2 Business Days" }], },
    CNH: { local: [], international: [{ rail: "SWIFT", cutoffTime: "9:00", deliveryTime: "1-3 Business Days" }], },
    CZK: { local: [{ rail: "Certis", cutoffTime: "9:00", deliveryTime: "Same Day" }], international: [{ rail: "SWIFT", cutoffTime: "9:00", deliveryTime: "1-2 Business Days" }], },
    DKK: { local: [{ rail: "Intradag", cutoffTime: "11:30", deliveryTime: "Same Day" }], international: [{ rail: "SWIFT", cutoffTime: "12:30, 08:00", deliveryTime: "1-2 Business Days" }], },
    EUR: { local: [{ rail: "SCT", cutoffTime: "12:30", deliveryTime: "Same Day / Next Day" }, { rail: "SCT Inst", cutoffTime: "24/7", deliveryTime: "Instant" }, { rail: "T2", cutoffTime: "16:15", deliveryTime: "Same Day (Real-time)" },], international: [{ rail: "SWIFT", cutoffTime: "16:00", deliveryTime: "1-2 Business Days" }], },
    GBP: { local: [{ rail: "FPS", cutoffTime: "24/7", deliveryTime: "Near Real-time" }, { rail: "CHAPS", cutoffTime: "17:45", deliveryTime: "Same Day" },], international: [{ rail: "SWIFT", cutoffTime: "17:45, 18:30", deliveryTime: "1-2 Business Days" }], },
    HKD: { local: [], international: [{ rail: "SWIFT", cutoffTime: "14:30", deliveryTime: "1-3 Business Days" }], },
    HUF: { local: [{ rail: "Intergiro", cutoffTime: "13:15", deliveryTime: "Same Day" }], international: [{ rail: "SWIFT", cutoffTime: "14:00", deliveryTime: "1-2 Business Days" }], },
    ILS: { local: [], international: [{ rail: "SWIFT", cutoffTime: "12:00", deliveryTime: "1-3 Business Days" }], },
    JPY: { local: [], international: [{ rail: "SWIFT", cutoffTime: "08:00, 17:00 D+1", deliveryTime: "1-3 Business Days" }], },
    MXN: { local: [], international: [{ rail: "SWIFT", cutoffTime: "16:30", deliveryTime: "1-3 Business Days" }], },
    NOK: { local: [{ rail: "NICS", cutoffTime: "11:30", deliveryTime: "Same Day" }], international: [{ rail: "SWIFT", cutoffTime: "14:30, 08:00", deliveryTime: "1-2 Business Days" }], },
    NZD: { local: [], international: [{ rail: "SWIFT", cutoffTime: "17:00 D+1", deliveryTime: "1-3 Business Days" }], },
    PLN: { local: [{ rail: "Sorbnet", cutoffTime: "16:00", deliveryTime: "Same Day" }], international: [{ rail: "SWIFT", cutoffTime: "11:45, 11:00", deliveryTime: "1-2 Business Days" }], },
    RON: { local: [{ rail: "ReGIS", cutoffTime: "12:15", deliveryTime: "Same Day" }], international: [{ rail: "SWIFT", cutoffTime: "11:30", deliveryTime: "1-2 Business Days" }], },
    SAR: { local: [], international: [{ rail: "SWIFT", cutoffTime: "8:00", deliveryTime: "1-3 Business Days" }], },
    SEK: { local: [{ rail: "RIX-Inst", cutoffTime: "24/7", deliveryTime: "Instant" }], international: [{ rail: "SWIFT", cutoffTime: "15:00, 08:00", deliveryTime: "1-2 Business Days" }], },
    SGD: { local: [], international: [{ rail: "SWIFT", cutoffTime: "18:30 D+1", deliveryTime: "1-3 Business Days" }], },
    TRY: { local: [], international: [{ rail: "SWIFT", cutoffTime: "10:00", deliveryTime: "1-3 Business Days" }], },
    USD: { local: [{ rail: "ACH", cutoffTime: "21:45", deliveryTime: "1-2 Business Days" }], international: [{ rail: "SWIFT", cutoffTime: "18:50, 18:30", deliveryTime: "1-3 Business Days" }], },
    ZAR: { local: [], international: [{ rail: "SWIFT", cutoffTime: "14:30, 09:30", deliveryTime: "1-3 Business Days" }], },
  };

  const sortedCurrencyCodes = Object.keys(paymentData).sort();
  const processedCurrencyData = sortedCurrencyCodes.map(code => ({
    value: code,
    label: `${code}`,
    details: paymentData[code]
  }));

  const currencyDropdownOptions = [
    {
      value: '',
      label: 'Select a currency...',
      details: { message: 'Select a currency to see its available payout routes.' },
      disabled: true
    },
    ...processedCurrencyData
  ];

  const [selectedValue, setSelectedValue] = useState(currencyDropdownOptions[0].value);
  const selectedOption = currencyDropdownOptions.find(opt => opt.value === selectedValue);

  const handleChange = (event) => {
    setSelectedValue(event.target.value);
  };

  return (
    <div style={{ border: '1px solid #e0e0e0', padding: '20px', borderRadius: '8px', marginBottom: '25px', backgroundColor: '#fdfdfd' }}>
      <label htmlFor="currency-routes-dropdown-headers" style={{ marginRight: '10px', fontWeight: '600', display: 'block', marginBottom: '8px' }}>
        Select currency to see available routes:
      </label>
      <select
        id="currency-routes-dropdown-headers"
        value={selectedValue}
        onChange={handleChange}
        style={{ padding: '10px', borderRadius: '4px', border: '1px solid #ccc', width: '100%', boxSizing: 'border-box', marginBottom: '20px' }}
      >
        {currencyDropdownOptions.map((option) => (
          <option key={option.value} value={option.value} disabled={option.disabled}>
            {option.label}
          </option>
        ))}
      </select>

      {selectedOption && selectedOption.details && (
        <div className="mt-4">
          {selectedOption.details.message ? (
            <p>{selectedOption.details.message}</p>
          ) : (
            <>

              {/* International Routes Section */}
              <div>
                <h5 className="text-lg font-medium mb-2 pb-1 border-b border-gray-300">Non-domestic</h5>
                {(!selectedOption.details.international || selectedOption.details.international.length === 0) ? (
                  <p className="text-gray-500 italic py-2">No non-domestic routes listed for this currency.</p>
                ) : (
                  <div>
                    {/* Column Headers for International Routes */}
                    <div className="grid grid-cols-4 gap-x-3 items-center py-2 border-b-2 border-gray-400 text-sm font-semibold text-gray-700 mb-1">
                      <div>Type</div>
                      <div>Name</div>
                      <div>Cutoff time (CET)</div>
                      <div>Delivery time (typical)</div>
                    </div>
                    {selectedOption.details.international.map((route, index) => (
                      <div
                        key={`${selectedOption.value}-International-${route.rail}-${index}`}
                        className="grid grid-cols-4 gap-x-3 items-center py-3 border-b border-gray-200 text-sm"
                      >
                        <div className="text-gray-600">Non-domestic</div>
                        <div className="font-medium text-gray-800">{route.rail}</div>
                        <div className="text-gray-700">{route.cutoffTime}</div>
                        <div className="text-gray-700">{route.deliveryTime}</div>
                      </div>
                    ))}
                  </div>
                )}
              </div>

              {/* Local Routes Section */}
              <div className="mt-5">
                <h5 className="text-lg font-medium mb-2 pb-1 border-b border-gray-300">Domestic</h5>
                {(!selectedOption.details.local || selectedOption.details.local.length === 0) ? (
                  <p className="text-gray-500 italic py-2">No domestic routes listed for this currency.</p>
                ) : (
                  <div>
                    {/* Column Headers for Local Routes */}
                    <div className="grid grid-cols-4 gap-x-3 items-center py-2 border-b-2 border-gray-400 text-sm font-semibold text-gray-700 mb-1">
                      <div>Type</div>
                      <div>Name</div>
                      <div>Cutoff time (CET)</div>
                      <div>Delivery time (typical)</div>
                    </div>
                    {selectedOption.details.local.map((route, index) => (
                      <div
                        key={`${selectedOption.value}-Local-${route.rail}-${index}`}
                        className="grid grid-cols-4 gap-x-3 items-center py-3 border-b border-gray-200 text-sm"
                      >
                        <div className="text-gray-600">Domestic</div>
                        <div className="font-medium text-gray-800">{route.rail}</div>
                        <div className="text-gray-700">{route.cutoffTime}</div>
                        <div className="text-gray-700">{route.deliveryTime}</div>
                      </div>
                    ))}
                  </div>
                )}
              </div>
            </>
          )}
        </div>
      )}
    </div>
  );
};

<CurrencyPayoutRoutesDropdown />