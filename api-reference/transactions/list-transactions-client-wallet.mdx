---  
 title: 'List Transactions for a Client Wallet'  
 api: 'GET /v2.01/{ClientId}/clients/wallets/{FundsType}/{Currency}/transactions'  
---  

import Array_TransactionsResponse from '/snippets/parameters/Array_TransactionsResponse.mdx';
import Currency20 from '/snippets/parameters/Currency20.mdx';
import FundsType from '/snippets/parameters/FundsType.mdx';
  

This call returns all the transactions targeting or emitted from the Client Wallet:  
-Fees wallet – Returns all the payouts made from the wallet.  
-Credit wallet – Returns the repudiations and settlements made from or to the wallet.  

### Path parameters  

<FundsType />  

<Currency20 />  

### Responses  

###### 200   

<Array_TransactionsResponse />  

 <ResponseExample>  

```json 200  

[
    {
        "Id": "163311508",
        "Tag": null,
        "CreationDate": 1677579888,
        "AuthorId": "ClientId",
        "CreditedUserId": "ClientId",
        "DebitedFunds": {
            "Currency": "XXX",
            "Amount": 0
        },
        "CreditedFunds": {
            "Currency": "XXX",
            "Amount": 0
        },
        "Fees": {
            "Currency": "XXX",
            "Amount": 0
        },
        "Status": "CREATED",
        "ResultCode": null,
        "ResultMessage": null,
        "ExecutionDate": null,
        "Type": "PAYIN",
        "Nature": "REGULAR",
        "CreditedWalletId": "CREDIT_EUR",
        "DebitedWalletId": null
    }
]  

 ```  

 </ResponseExample>  

<RequestExample>  
```  
javascript NodeJS  

const mangopayInstance = require('mangopay2-nodejs-sdk')
const mangopay = new mangopayInstance({
    clientId: 'your-client-id',
    clientApiKey: 'your-api-key',
})

let myWallet = {
  FundsType: 'FEES',
  Currency: 'EUR',
}

const listTransactionsClientWallet = async (fundsType, currency) => {
  return await mangopay.Clients.getClientWalletTransactions(fundsType, currency)
    .then((response) => {
      console.info(response)
      return response
    })
    .catch((err) => {
      console.log(err)
      return false
    })
}

listTransactionsClientWallet(myWallet.FundsType, myWallet.Currency)  
```  

  
</RequestExample>